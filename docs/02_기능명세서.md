# 체인아이(ChainEye) 기능명세서

**2026 금융 AI Challenge (금융보안원) 제출용**
**서비스명:** 체인아이(ChainEye) — 가상자산 자금세탁 탐지·추적 AI 웹서비스
**문서 버전:** 1.0 · **작성일:** 2026-08-13

---

## 1. 문서 개요

### 1.1 서비스명 및 목적
체인아이(ChainEye)는 비트코인 트랜잭션 그래프에서 자금세탁 의심 거래를 탐지하고, 대상 거래의 상·하류 자금흐름을 시각적으로 추적하며, 판정 근거(XAI)와 함께 수사·컴플라이언스 담당자용 한국어 리포트를 자동 생성하는 웹서비스이다. 본 문서는 **이미 구현·동작 중인 서비스**의 기능을 명세하는 것을 목적으로 한다.

### 1.2 문서 범위
본 명세서는 체인아이의 실제 구현물(프론트엔드, 백엔드 API, ML 추론 모듈, 학습 모델·데이터 산출물)을 대상으로 다음을 기술한다.
- 시스템 구성 및 계층 구조
- 제공 기능 목록과 기능별 상세 명세
- REST API 엔드포인트별 요청/응답 규격
- 학습 데이터 및 AI 모델 구성·성능
- 화면 구성 및 비기능 요구사항, 제약사항·향후계획

본 문서에 기술된 성능지표·API 규격·데이터 수치는 실제 소스코드 및 산출물(`app/ml/metrics.json`, `app/backend/main.py`, `app/ml/inference.py`)과 교차 검증된 값이다.

### 1.3 용어 정의

| 용어 | 정의 |
|---|---|
| txId | 비트코인 트랜잭션 식별자. Elliptic 데이터셋에서는 익명화된 정수 ID이다. |
| illicit / licit | 불법(자금세탁 의심) / 합법(정상) 거래 라벨 |
| 리스크 스코어(riskScore) | 모델이 산출한 불법 확률을 0~100 정수로 환산한 위험 점수 |
| SHAP | 모델 판정에 대한 각 피처의 기여도를 산출하는 설명가능성(XAI) 기법 |
| topFactors | 특정 판정에서 기여도 상위 피처 목록(부호 있는 SHAP 값 포함) |
| hop | 그래프 추적 시 대상 노드로부터의 이웃 확장 단계 수 |
| 템포럴 분할(temporal split) | 시간축(타임스텝) 기준 학습/검증 분할. 무작위 분할과 달리 미래 시점 데이터 누수를 방지 |
| MODEL / MOCK 모드 | 실제 학습 모델로 응답하는 모드 / 모델 미로딩 시 동일 스키마의 샘플 데이터로 응답하는 폴백 모드 |
| STR | 의심거래보고(Suspicious Transaction Report) |

---

## 2. 시스템 구성도

체인아이는 프론트엔드 → 백엔드 API → 모델·데이터 계층의 3계층 구조이다.

```
┌──────────────────────────────────────────────────────────────┐
│  [프레젠테이션 계층]  React + Vite SPA  (http://localhost:5173) │
│   InputBar · RiskPanel · GraphPanel(Cytoscape.js) · ReportPanel│
│   config.js(API_BASE / USE_MOCK) · api.js(fetch 래퍼)          │
└───────────────────────────┬──────────────────────────────────┘
                            │  HTTP / JSON (CORS 허용)
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  [애플리케이션 계층]  FastAPI 백엔드  (http://localhost:8000)   │
│   GET  /health   상태 · 모델 로드 여부                          │
│   POST /score    위험 점수 + 라벨 + topFactors(SHAP)           │
│   POST /trace    자금흐름 서브그래프(nodes/edges)              │
│   POST /explain  판정 근거(topFactors) 조회                    │
│   POST /report   템플릿 기반 한국어 분석 보고서                │
│   · 기동 시 ML 모듈 import 시도 → 성공: MODEL / 실패: MOCK 폴백 │
└───────────────────────────┬──────────────────────────────────┘
                            │  in-process 함수 호출 (loose coupling)
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  [모델·데이터 계층]  app/ml/inference.py                       │
│   chaineye_model.pkl (LightGBM)  ·  shap.TreeExplainer         │
│   feature_table.parquet (203,769 노드 × 165 피처)             │
│   edges.parquet (방향 엣지) → 무방향 인접리스트(그래프 추적)   │
└──────────────────────────────────────────────────────────────┘
```

**연동 특성(loose coupling):** 백엔드는 기동 시 `app/ml/inference.py` 를 import·load 시도한다. 성공하면 `/score`·`/trace` 는 실제 모델로 위임(MODEL 모드)되고, 실패하면 동일 스키마의 샘플 데이터를 반환하는 내장 mock provider로 폴백(MOCK 모드)하여 ML 준비 여부와 무관하게 API가 항상 응답한다.

---

## 3. 기능 목록

| 기능ID | 기능명 | 설명 | 관련 API |
|---|---|---|---|
| F-01 | 리스크 스코어링 | 입력 트랜잭션의 불법 확률을 0~100 위험 점수와 라벨(illicit/licit)로 산출 | POST /score |
| F-02 | 자금흐름 추적 | 대상 노드의 상·하류 이웃을 hop 단위로 확장한 서브그래프(노드·엣지) 생성 | POST /trace |
| F-03 | 설명가능성(XAI) | SHAP 기반으로 판정 기여도 상위 피처(부호 포함)를 제시 | POST /explain, /score |
| F-04 | 자동 보고서 | 점수·근거·그래프 통계를 종합한 한국어 수사·컴플라이언스 보고서 생성 | POST /report |
| F-05 | 그래프 시각화 | Cytoscape.js로 자금흐름 그래프를 렌더링하고 위험 노드를 색상 강조 | (프론트엔드) |
| F-06 | 상태 점검(헬스체크) | 서비스 상태 및 실제 모델 로드 여부(MODEL/MOCK) 확인 | GET /health |
| F-07 | 데모/실서비스 전환 | `USE_MOCK` 설정으로 백엔드 없이도 UI 전체 시연 가능 | (프론트/백엔드 설정) |
| F-08 | 의심 자금세탁 경로 탐지·시각화 | 대상 거래에서 출발해 고위험(위험도 70+) 노드에서 끝나는 방향성 자금 이동 경로를 탐지하고 그래프에서 강조 | POST /trace |

---

## 4. 기능별 상세 명세

### F-01 리스크 스코어링
- **입력:** 트랜잭션 ID(txId, 문자열)
- **처리:**
  1. txId를 정수로 변환 후 피처 테이블(`feature_table.parquet`)에서 165차원 피처 벡터 조회
  2. LightGBM `predict_proba` 로 불법 확률 산출, `round(proba × 100)` 로 0~100 위험 점수 환산
  3. 임계값 0.5 기준으로 라벨 결정(`illicit` / `licit`)
  4. `shap.TreeExplainer` 로 해당 예측의 피처 기여도 산출, 절댓값 상위 6개를 topFactors로 반환
  - 피처가 없는(테이블 미등록) txId는 예외 없이 `riskScore 0`, `label "licit"`, `topFactors []` 반환
- **출력:** riskScore(0~100), label, topFactors(최대 6개, 부호 있는 SHAP impact)
- **화면:** 위험도 패널(좌) — SVG 게이지 + 등급 배지 + 핵심 위험 근거 목록
- **관련 API:** `POST /score`

### F-02 자금흐름 추적
- **입력:** txId, hops(1~4, 기본 2)
- **처리:**
  1. 방향 엣지리스트로부터 구성한 무방향 인접리스트에서 대상 노드 기준 BFS로 hop 단계까지 이웃 확장
  2. 노드 수가 상한(약 60개)을 초과하면 위험 점수 높은 이웃 우선으로 절단(대상 노드는 항상 포함)
  3. 각 노드에 위험 점수 부여, `focus` 플래그로 대상 노드 표시
  4. 유지된 노드 집합 내부의 엣지만(원본 방향 유지) 반환
  - 그래프에 없는 노드는 대상 노드 단독(엣지 없음)으로 반환
- **출력:** nodes(id, risk 0~100, focus, illicit), edges(source, target), paths(List[List[str]])
- **화면:** 자금 흐름 그래프 패널(중앙) — Cytoscape.js 방향 그래프
- **관련 API:** `POST /trace`

### F-03 설명가능성(XAI)
- **입력:** txId
- **처리:** 스코어링 provider의 SHAP 기여도(topFactors)를 재사용하여 판정 근거만 반환. 각 항목은 피처명과 부호 있는 기여도(impact)로 구성되며, 양수는 위험도 상승, 음수는 하락 방향을 의미
- **출력:** txId, topFactors
- **화면:** 위험도 패널의 "핵심 위험 근거" 영역 및 리포트의 【핵심 판단 근거】 절
- **관련 API:** `POST /explain` (내부적으로 `/score` provider 재사용)

### F-04 자동 보고서
- **입력:** txId, score(0~100), label, topFactors, graphStats(nodeCount, edgeCount, highRiskCount, hops; 구 스키마 illicitNeighbors 폴백 호환)
- **처리:** `report_builder.build_report` 가 템플릿 기반으로 한국어 보고서를 결정론적으로 렌더링. 점수를 등급(70↑ 위험 / 40↑ 주의 / 그 외 안전)으로 매핑하고, 위험 요약·핵심 판단 근거(상위 5개)·자금흐름 관찰·등급별 권고 조치(STR 검토, 모니터링 강화 등) 절을 구성. 외부 유료 LLM API는 사용하지 않음(현재 MVP 기준)
- **출력:** report(문자열, 구획선 포함 서식 텍스트)
- **화면:** AI 조사 리포트 패널(우) — 경량 마크다운 렌더링
- **관련 API:** `POST /report`

### F-05 그래프 시각화
- **입력:** `/trace` 응답(nodes/edges)
- **처리:** Cytoscape.js로 방향 그래프 렌더링. 대상/고위험 노드는 빨강, 일반 노드는 회색으로 표시하여 위험 경로를 시각적으로 강조
- **출력:** 인터랙티브 그래프 뷰
- **화면:** 자금 흐름 그래프 패널(중앙, `GraphPanel.jsx`)

### F-06 상태 점검(헬스체크)
- **입력:** 없음
- **처리:** 서비스 상태와 실제 모델 로드 여부(`modelLoaded`) 반환. MODEL/MOCK 모드 판별에 사용
- **출력:** status, modelLoaded
- **관련 API:** `GET /health`

### F-07 데모/실서비스 전환
- **처리:** 프론트엔드 `src/config.js` 의 `USE_MOCK` 플래그로 샘플 데이터(백엔드 불필요)와 실제 API 호출을 전환. 백엔드도 모델 미로딩 시 MOCK 모드로 자동 폴백하여 이중 안전장치 제공
- **관련 설정:** `config.js`(프론트), `STATE.mode`(백엔드)

### F-08 의심 자금세탁 경로 탐지·시각화
- **입력:** 대상 txId, hops(1~4)
- **처리:**
  1. `/trace` 로 구성된 추적 이웃(노드 상한 60개) 위에서, 유지된 노드 간 **원본 방향(directed) 엣지**로 방향성 인접리스트를 구성
  2. 대상(focus) 노드에서 출발하는 **방향성 워크(directed walk)** 탐색으로, 고위험 노드(위험도 70 이상)에서 끝나는 경로를 수집. 경로 길이는 노드 기준 2 ~ hops+1(엣지 1 ~ hops)이며, 단일 경로 내 사이클은 배제
  3. 중복 경로를 제거하고 최대 8개까지 반환
  4. 각 노드에는 위험도 70 이상 여부를 나타내는 `illicit`(bool) 플래그를 함께 부여
- **출력:** 응답 최상위 `paths`(List[List[str]]) — txId 문자열의 순서 있는 목록. 대상 노드에서 시작해 고위험 노드에서 종료. 각 노드의 `illicit` 플래그 포함
- **화면:** 자금 흐름 그래프 패널(중앙, `GraphPanel.jsx`) — 경로 엣지를 주황색 marching-ants(행진 개미) 애니메이션으로 강조하고 범례에 "의심 자금세탁 경로" 항목 표시
- **관련 API:** `POST /trace`

---

## 5. API 명세

- **Base URL:** `http://localhost:8000`
- **인코딩:** 요청·응답 모두 `application/json`
- **CORS 허용 오리진:** `http://localhost:5173`, `http://localhost:3000`
- **API 버전:** 0.1.0 · **문서(Swagger):** `/docs`

### 5.1 GET /health — 상태 점검
- **목적:** 서비스 및 모델 로드 여부 확인

**응답 필드**

| 필드 | 타입 | 설명 |
|---|---|---|
| status | string | 서비스 상태(기본 "ok") |
| modelLoaded | boolean | 실제 ML 모델 로드 여부(true=MODEL, false=MOCK) |

**예시**
```
GET /health
→ {"status": "ok", "modelLoaded": true}
```

### 5.2 POST /score — 리스크 스코어링
- **목적:** 단일 거래의 위험 점수·라벨·판정 근거 산출

**요청 필드**

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| txId | string | Y | 점수 산출 대상 트랜잭션 ID |

**응답 필드**

| 필드 | 타입 | 설명 |
|---|---|---|
| txId | string | 대상 트랜잭션 ID |
| riskScore | int(0~100) | 위험 점수 |
| label | "illicit" \| "licit" | 판정 라벨(임계값 0.5) |
| topFactors | array | 기여도 상위 피처(최대 6개) |
| topFactors[].feature | string | 피처명(예: feat_58) |
| topFactors[].impact | float | 부호 있는 SHAP 기여도 |

**예시**
```
POST /score  {"txId": "232629023"}
→ {
    "txId": "232629023",
    "riskScore": 98,
    "label": "illicit",
    "topFactors": [
      {"feature": "feat_1", "impact": 1.18},
      {"feature": "feat_58", "impact": 0.62}
    ]
  }
```

### 5.3 POST /trace — 자금흐름 추적
- **목적:** 대상 노드 기준 이웃 서브그래프 생성

**요청 필드**

| 필드 | 타입 | 필수 | 기본 | 설명 |
|---|---|---|---|---|
| txId | string | Y | — | 추적 시작 트랜잭션 ID |
| hops | int(1~4) | N | 2 | 이웃 확장 단계 수 |

**응답 필드**

| 필드 | 타입 | 설명 |
|---|---|---|
| nodes | array | 노드 목록(최대 약 60개) |
| nodes[].id | string | 노드(트랜잭션) ID |
| nodes[].risk | int(0~100) | 노드 위험 점수 |
| nodes[].focus | boolean | 대상(중심) 노드 여부 |
| nodes[].illicit | boolean | 고위험 여부(위험도 70 이상) |
| edges | array | 유지된 노드 간 방향 엣지 |
| edges[].source | string | 출발 노드 ID |
| edges[].target | string | 도착 노드 ID |
| paths | array(List[List[str]]) | 의심 자금세탁 경로. 대상 노드에서 출발해 고위험 노드에서 끝나는 방향성 워크(길이 2~hops+1 노드), 중복 제거 후 최대 8개 |

**예시**
```
POST /trace  {"txId": "232629023", "hops": 2}
→ {
    "nodes": [
      {"id": "232629023", "risk": 98, "focus": true, "illicit": true},
      {"id": "231981037", "risk": 61, "focus": false, "illicit": false},
      {"id": "231044559", "risk": 84, "focus": false, "illicit": true}
    ],
    "edges": [
      {"source": "232629023", "target": "231981037"},
      {"source": "231981037", "target": "231044559"}
    ],
    "paths": [
      ["232629023", "231981037", "231044559"]
    ]
  }
```

### 5.4 POST /explain — 판정 근거 조회
- **목적:** 특정 거래의 SHAP 기여도 상위 피처 조회

**요청 필드**

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| txId | string | Y | 설명 대상 트랜잭션 ID |

**응답 필드**

| 필드 | 타입 | 설명 |
|---|---|---|
| txId | string | 대상 트랜잭션 ID |
| topFactors | array | 기여도 상위 피처(feature, impact) — `/score` 와 동일 스키마 |

**예시**
```
POST /explain  {"txId": "232629023"}
→ {"txId": "232629023",
   "topFactors": [{"feature": "feat_58", "impact": 0.62}, ...]}
```

### 5.5 POST /report — 자동 보고서 생성
- **목적:** 점수·근거·그래프 통계를 종합한 한국어 분석 보고서 생성

**요청 필드**

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| txId | string | Y | 대상 트랜잭션 ID |
| score | int(0~100) | Y | 위험 점수 |
| label | string | Y | 라벨(illicit/licit) |
| topFactors | array | N | 판정 근거(feature, impact), 기본 [] |
| graphStats | object | N | 그래프 통계 |
| graphStats.nodeCount | int | N | 추적 노드 수(기본 0) |
| graphStats.edgeCount | int | N | 자금 이동(엣지) 건수(기본 0). 보고서의 "자금 이동 N건" 표기에 사용 |
| graphStats.highRiskCount | int | N | 고위험(위험도 70+) 노드 수(기본 0). 프론트엔드가 전송하는 위험 이웃 지표 |
| graphStats.illicitNeighbors | int | N | (구 스키마) 위험 이웃 노드 수. 전달되면 우선 사용, 없으면 highRiskCount 로 폴백 |
| graphStats.hops | int | N | 추적 hop 수(기본 0) |

> **graphStats 처리(정합화됨):** `report_builder` 는 위험 이웃 수를 `illicitNeighbors`(있으면) 또는 `highRiskCount` 값으로 산정한다. 대상(focus) 노드 자신이 고위험(점수 70+)이면 자기 자신은 이웃이 아니므로 카운트에서 1을 차감한다. `edgeCount` 가 있으면 "자금 이동 N건"을 함께 표기한다.

**응답 필드**

| 필드 | 타입 | 설명 |
|---|---|---|
| report | string | 서식화된 한국어 보고서 텍스트 |

**예시**
```
POST /report  {
  "txId": "232629023", "score": 98, "label": "illicit",
  "topFactors": [{"feature": "feat_58", "impact": 0.62}],
  "graphStats": {"nodeCount": 41, "edgeCount": 58, "highRiskCount": 8, "hops": 2}
}
→ {"report": "━━━ 체인아이(ChainEye) 자금세탁 위험 분석 보고서 ━━━ ..."}
```

---

## 6. 데이터 명세

### 6.1 Elliptic 데이터셋 개요
Elliptic사가 공개한 익명화된 실제 비트코인 트랜잭션 그래프를 학습·검증에 사용한다.

| 항목 | 값 |
|---|---|
| 노드(트랜잭션) 수 | 203,769 |
| 엣지(비트코인 흐름) 수 | 234,355 |
| 타임스텝 수 | 49 |
| 노드당 피처 수 | 166 (거래 자체 정보 94 + 이웃 집계 72) |
| 라벨 분포 | 불법 2%(4,545) / 합법 21%(42,019) / 미분류 77% |

### 6.2 전처리 및 산출물
학습 파이프라인(`app/ml/train.py`)은 원본 Elliptic CSV(features.csv 약 665MB)를 float32 다운캐스팅으로 1회 읽어 다음 산출물을 생성한다(전체 약 30초).

| 산출물 | 용도 |
|---|---|
| `chaineye_model.pkl` | 학습된 LightGBM 분류기(joblib) |
| `feature_table.parquet` | 전체 203,769 노드의 165 피처(txId 인덱스, 추론용) |
| `edges.parquet` | 방향 엣지리스트(txId1 → txId2) |
| `shap_background.parquet` | SHAP 배경 표본 |
| `feature_importance.csv` | 전역 SHAP 평균 절댓값 중요도 |
| `metrics.json` | 평가 지표 |

> **피처 수 주석:** 데이터셋 원본 피처는 166개이나, 추론 모듈(`inference.py`)은 시간축 컬럼(time_step)을 제외한 **165개 피처**(`feat_0`~`feat_164`)를 모델 입력으로 사용한다. `feat_i` 는 익명화된 원본 피처의 i번째 컬럼을 의미한다.

### 6.3 학습/테스트 분할 (템포럴 분할)
데이터 누수를 방지하기 위해 무작위 분할이 아닌 **시간축 기준 분할**을 적용한다.

| 구분 | 타임스텝 | 라벨 표본 수 | 불법 표본 수 |
|---|---|---|---|
| 학습(train) | 1 ~ 34 | 29,894 | 3,462 |
| 테스트(test) | 35 ~ 49 | 16,670 | 1,083 |

미분류(unknown) 라벨은 학습·평가에서 제외하되, 피처가 있는 미분류 거래도 추론 시점에는 정상적으로 점수화된다.

---

## 7. AI 모델 명세

### 7.1 모델 구성
- **알고리즘:** LightGBM 이진 분류기(불법=양성 클래스)
- **입력:** 165차원 피처 벡터
- **출력:** 불법 확률(0~1) → 0~100 위험 점수 및 임계값 0.5 기준 라벨
- **클래스 불균형 대응:** `scale_pos_weight = 7.63`
- **최적 반복(best_iteration):** 188
- **설명가능성:** `shap.TreeExplainer` — 예측별 피처 기여도(부호 포함) 및 전역 중요도 산출

### 7.2 성능 지표 (테스트셋, 템포럴 분할, 불법=양성)

| 지표 | 값 |
|---|---|
| Illicit Precision | 0.8259 |
| Illicit Recall | 0.7313 |
| **Illicit F1 (대표 지표)** | **0.7757** |
| PR-AUC (Average Precision) | 0.7970 |
| ROC-AUC | 0.9363 |

불균형 데이터 특성상 Accuracy가 아닌 **불법 클래스 기준 F1·PR-AUC**를 대표 지표로 사용한다. 대표 지표 Illicit-F1 0.776은 공개된 LightGBM/GBT 베이스라인 범위(약 0.70~0.80) 내에 위치한다.

**혼동 행렬 (임계값 0.5, 행=실제 / 열=예측)**

|  | 예측 licit | 예측 illicit |
|---|---:|---:|
| 실제 licit | 15,420 (TN) | 167 (FP) |
| 실제 illicit | 291 (FN) | 792 (TP) |

### 7.3 SHAP 기반 설명
- **전역 중요도 상위 피처:** feat_58, feat_52, feat_75, feat_57, feat_4, feat_124
- **예측별 설명:** `/score`·`/explain` 응답의 topFactors로 절댓값 상위 6개 피처를 부호 있는 기여도와 함께 제공. 양수는 위험도 상승, 음수는 하락 방향으로 해석된다.

### 7.4 그래프 신경망(GCN) 보완 모델

LightGBM이 각 트랜잭션을 독립적인 피처 벡터로 다루는 것과 달리, GCN은 트랜잭션 그래프를 따라 이웃 노드의 피처를 전파(propagation)하여 "누가 누구와 거래하는가"의 구조적 신호를 학습한다. 이는 레이어링·필체인·믹서 팬아웃 등 자금세탁 유형(typology)을 그래프 구조 관점에서 포착하기 위한 **보완 모델**로 구현되었다.

- **위치:** `app/ml/gcn/`(`gcn_train.py`, `gcn_model.pt`, `gcn_metrics.json`, `README.md`)
- **구성:** 3× GCNConv(165 → 128 → 128 → 2), ReLU, dropout 0.3
- **학습:** 클래스 가중 교차엔트로피(불법 가중 ≈ 7.63), 400 epoch, Adam(lr 0.01, wd 5e-4), full-batch CPU
- **분할:** LightGBM과 동일한 템포럴 분할(train time_step 1~34 / test 35~49), 그래프 468,710 무방향 엣지

**모델 비교 (테스트셋, 템포럴 분할, 불법=양성)**

| 모델 | Illicit Precision | Illicit Recall | Illicit F1 | ROC-AUC | 역할 |
|---|:---:|:---:|:---:|:---:|---|
| **LightGBM** | 0.826 | 0.731 | **0.776** | **0.936** | PRIMARY(라이브 스코어링) |
| GCN | 0.561 | 0.563 | 0.562 | 0.892 | 보완/앙상블 후보 |

> **구현 정합성(중요):** 현재 **라이브 스코어링(`/score`·`/trace`)은 LightGBM 단일 모델로만** 수행된다. GCN은 `app/ml/gcn/` 의 **독립(standalone) 연구 산출물**이며 `inference.py`·백엔드에 연결(wiring)되어 있지 **않다**. GCN은 그래프 구조적 위험을 인코딩하는 보완 모델 / 앙상블 후보로 제공된다. F1(0.562)은 LightGBM(0.776)보다 낮으나, Elliptic의 잘 알려진 패턴(정교하게 설계된 피처를 트리 모델이 직접 활용)에 부합하며, ROC-AUC 0.892는 그래프가 실질 신호를 담고 있음을 보여준다 — 다음 단계는 두 모델의 앙상블 또는 GraphSAGE/GAT로의 고도화이다.

---

## 8. 화면 구성

프론트엔드(Vite + React + Cytoscape.js SPA, 기본 `http://localhost:5173`)는 상단 입력 영역과 3분할 결과 패널로 구성된다. 사용자가 txId를 입력해 "분석"을 실행하면 `/score`·`/trace` 를 병렬 호출하고, 그 결과로 그래프 통계를 계산해 `/report` 를 호출한다.

| 구성 영역 | 컴포넌트 | 설명 |
|---|---|---|
| ① 입력바 | `InputBar.jsx` | txId 입력 필드 + "분석" 버튼 + 예시 txId 칩(클릭 시 자동 입력) |
| ② 위험도 패널(좌) | `RiskPanel.jsx` | 0~100 위험 점수 SVG 게이지 + 등급 라벨(안전/주의/위험) + 핵심 위험 근거 |
| ③ 자금 흐름 그래프(중앙) | `GraphPanel.jsx` | Cytoscape.js 방향 그래프. 대상/고위험 노드 빨강, 일반 노드 회색 |
| ④ AI 조사 리포트(우) | `ReportPanel.jsx` | 자동 생성된 한국어 조사 리포트(경량 마크다운 렌더링) |

상단 헤더에는 현재 동작 모드(MOCK 데모 / LIVE API) 태그가 표시된다. 설정은 `src/config.js` 한 파일(API_BASE, USE_MOCK, DEFAULT_HOPS, 예시 txId)에 집약되어 있다.

> **주석:** 입력바를 포함하면 화면은 4개 영역으로 구성되나, 결과 표시 패널은 위험도·그래프·리포트의 3패널이다.

---

## 9. 비기능 요구사항

| 구분 | 내용 |
|---|---|
| 성능 | 추론 결과 `lru_cache`(최대 100,000건) 캐싱으로 반복 조회 가속. 그래프 추적 노드 수 상한 약 60개로 응답 크기·렌더링 부하 제한. 학습 산출물 재생성 약 30초 |
| 안정성 | 미등록 txId·그래프 미포함 노드는 예외 없이 안전 기본값 반환. ML 모듈 미로딩 시 MOCK 모드 자동 폴백으로 API 무중단 응답 |
| 보안 | CORS 허용 오리진을 로컬 개발 오리진으로 제한. 외부 유료 LLM API 호출 없이 온프레미스 템플릿으로 보고서 생성(데이터 외부 유출 최소화) |
| 확장성 | 백엔드-ML 느슨한 결합(loose coupling)으로 모델 교체·고도화 시 API 스키마 유지. 프론트 `USE_MOCK` 분기로 백엔드 독립 시연 가능 |
| 사용성 | 예시 txId 칩, 등급별 색상 코딩, 한국어 리포트로 비전문 분석관도 즉시 활용 가능 |

---

## 10. 제약사항 및 향후계획

### 10.1 현재 제약사항 (구현 범위 명시)
- **그래프 신경망(GCN)은 구현되었으나 라이브 미연결:** GCN 보완 모델은 `app/ml/gcn/` 에 학습·평가까지 구현되었다(7.4 참조). 다만 **라이브 스코어링은 여전히 LightGBM 단일 모델로만** 동작하며, GCN은 `inference.py`·백엔드에 연결되지 않은 독립 산출물(앙상블 후보)이다. 2트랙 앙상블 연동은 미구현이다.
- **자동 보고서(LLM):** 현재 `/report` 는 **템플릿 기반 결정론적 생성기**(`report_builder.py`)이며 외부 LLM을 호출하지 않는다. Claude API 연동 지점은 코드에 `TODO(claude-api)` 로 표시되어 있으나 MVP 요건상 미구현이다.
- **피처 한글 매핑:** 보고서 생성기는 일부 의미형 피처명(예: mixer_exposure_ratio)에 대한 한글 라벨 사전을 갖고 있으나, 실제 모델의 익명화 피처명(feat_i)은 사전에 없어 원문(feat_i)으로 표기된다.
- **`/report` 그래프 통계 필드 정합화(해결됨):** 이전에 보고된 프론트-백엔드 필드명 불일치(프론트가 `highRiskCount` 전송 → 백엔드가 `illicitNeighbors` 만 인식하여 위험 이웃 수가 항상 0으로 처리되던 문제)는 **해결되었다**. 백엔드 `GraphStats` 는 이제 `{nodeCount, edgeCount, highRiskCount, hops}` 를 수용하고, `report_builder` 는 `highRiskCount`(구 `illicitNeighbors` 우선) 를 위험 이웃 수로 사용하되 대상 노드 자신이 고위험이면 1을 차감하며, `edgeCount` 를 "자금 이동 N건"으로 함께 표기한다.
- **배포:** 로컬 개발 환경(프론트 :5173, 백엔드 :8000) 기준으로 기술되었으며, 공개 URL 배포(Hugging Face Spaces/Render)는 기획 로드맵 항목이다.

### 10.2 향후계획
- 구현된 GCN 보완 모델을 라이브 추론에 연결하고 LightGBM과의 2트랙 앙상블로 "돈의 흐름" 자체를 학습하는 서사 강화(GraphSAGE/GAT 고도화 포함)
- Claude API 연동으로 템플릿 보고서를 서술형 내러티브 리포트로 확장(템플릿은 오프라인·레이트리밋 시 결정론적 폴백으로 유지)
- Elliptic++ / Elliptic2 등 대규모·서브그래프 데이터로 확장 및 공개 URL 배포

---

*본 명세서는 실제 소스코드 및 산출물과 교차 검증하여 작성되었으며, 코드와 기획 문서 간 차이가 있는 항목은 코드 구현을 기준으로 기술하고 제10장에 별도 명시하였다.*
