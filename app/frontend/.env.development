# 개발 서버(`npm run dev`) 기본값 — 독립 mock 데모.
# 백엔드 없이 하드코딩된 샘플 데이터로 UI 전체를 시연할 수 있습니다.
VITE_USE_MOCK=true

# 로컬에서 실제 백엔드로 개발할 때의 베이스 URL.
# (USE_MOCK=true 인 동안에는 사용되지 않지만, VITE_USE_MOCK=false 로 바꾸면 즉시 유효)
VITE_API_BASE=http://localhost:8000
