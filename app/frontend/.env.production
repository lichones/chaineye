# 프로덕션 빌드(`npm run build`) 기본값 — 실제 백엔드, 동일 출처(same-origin).
# 백엔드가 dist/ 를 정적 파일로 서빙하면 API 호출이 같은 도메인으로 나갑니다.
#   VITE_API_BASE='' (빈 값) => POST /score 는 배포 origin 기준 상대경로.
VITE_USE_MOCK=false
VITE_API_BASE=
